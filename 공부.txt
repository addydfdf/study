📁 중첩클래스와 중첩인터페이스
중첩클래스란?

클래스 내부에 선언한 클래스
사용 시 두 클래스의 멤버변수들을 서로 쉽게 접근 가능
코드의 복잡성을 줄일 수 있음
중첩인터페이스란?

클래스 내부에 인터페이스를 선언한 것
중첩클래스
클래스 내부에 선언되는 위치에 따라 두 가지로 분류


멤버 클래스

클래스 멤버로서 선언되는 중첩클래스
로컬클래스

생성자 또는 메서드 내부에서 선언된 중첩 클래스
중첩클래스도 하나의 클래스 -> 컴파일 할 경우



🖍️인스턴스 멤버 클래스
인스턴스 멤버 클래스 static 키워드 없이 중첩 선언된 클래스
인스턴스 필드와 메소드만 선언이 가능함, 정적 필드와 메소드 선언X

🖍️외부에서 A객체를 생성하려면?

A클래스 외부에서 B 객체를 생성하려면 먼저 A 객체를 생성하고 B 객체를 생성해야 함.

외부에서 객체를 생성하는 일은 거의 X
🖍️내부에서 A객체를 생성하려면?


일반 클래스처럼 객체 생성!
🖍️정적 멤버 클래스
static 키워드로 선언된 클래스
모든 종류의 필드와 메소드 선언 가능

A클래스 외부에서 static 멤버 클래스 C객체를 생성하려면?
->A객체 생성X
-> 바로 C객체 생성!


🖍️ 로컬클래스
메소드 내에서 중첩클래스를 선언한 것
로컬 클래스는 접근 제한자 및 static을 붙일 수x
-> 로컬 클래스는 메소드 내부에서만 사용 하기 때문에 접근을 제한할 필요 x
로컬 클래스 내부에는 인스턴스 필드와 메소드만 선언 가능(정적x)
로컬 클래스는 메소드가 실행될 때 메소드 내에서 객체를 생성하고 사용!

🖍️ 중첩 클래스의 접근 제한
🟧 바깥 필드와 메소드에서 사용 제한


인스턴스 멤버 클래스(B)는 바깥 클래스의 인스턴스 필드의 초기값이나 인스턴스 메소드에서 객체를 생성할 수 있으나 정적 필드의 초기값이나 정적 메소드에서는 객체를 생성X
정적 멤버 클래스(C)는 모든 필드의 초기값이나 모든 메소드에서 객체를 생성가능
🟧 멤버 클래스에서 사용 제한


멤버클래스 내부에서 바깥 클래스의 필드와 메소드 접근 시 제한
인스턴스 멤버 클래스 안에서는 바깥 클래스의 모든 필드와 모든 메소드에 접근가능
정적 멤버 클래스 안에서는 바깥 클래스의 정적 필드와 메소드에만 접근 가능, 인스턴스 필드와 메소드는 접근 X
🟧 로컬 클래스에서 사용 제한

로컬 클래스 내부에서는 바깥 클래스의 필드/메소드를 제한 없이 사용 가능.
문제는 메소드의 매개 변수나 로컬 변수를 로컬 클래스에서 사용할 때

로컬 클래스의 객체는 메소드 실행이 끝나도 힙 메모리에 존재해 계속 사용가능.
매개 변수나 로컬 변수는 메소드 실행이 끝나면 스택 메모리에서 사라지기 때문에 로컬 객체에서 사용할 경우 문제가 발생
--> 컴파일 시 로컬 클래스에서 사용하는 매개 변수, 로컬 변수의 값을 로컬 클래스 내부에 복사해 두고 사용.
그리고 매개 변수, 로컬 변수가 수정 시, 로컬 클래스에 복사해둔 값과 달라지는 문제를 해결위해 매개 변수, 로컬 변수를 final로 선언해서 수정 하는 것을 막음.

결론적으로 로컬 클래스에서 사용 가능한 것은 final로 선언된 매개 변수, 로컬 변수


🟧 중첩 클래스에서 바깥 클래스 참조얻기

클래스 내부에서 this는 객체 자신의 참조
중첩 클래스에서 this는 중첩 클래스의 객체 참조
중첩 클래스 내부에서 바깥 클래스의 객체 참조를 얻으려면 바깥 클래스의 이름을 this앞에 붙여야함.

🖍️ 중첩 인터페이스
클래스 멤버로 선언된 인터페이스를 말함.

클래스 내부에 선언하는 이유? ->해당 클래스와 긴밀한 관계를 맺는 구현 클래스 만들기 위함.

인스턴스 멤버 인터페이스와, 정적멤버 인터페이스 모두 가능

인스턴스 멤버 인터페이스는 바깥 클래스의 객체가 있어야 사용 가능.

정적 멤버 인터페이스는 바깥 클래스의 객체 없이 바깥 클래스만으로 접근 가능

UI 프로그래밍에서 이벤트를 처리할 목적으로 많이 활용.



Button의 중첩 인터페이스인 OnClickListner를 구현한 2 개의 클래스
